<div id="header">
    <img src="/imagenes/logo_grande.jpg" alt="logo"/><br>
    <font face= "tahoma">Últimas noticias</font>
</div>

<% @posts.each do |post| %>
    <div class="post_wrapper">
        <h2 class="title">
            <%= link_to post.title, post %>
        </h2>
        <p class="date">
            <%= post.created_at.strftime("%B, %d, %Y") %>
        </p>
    </div>
<% end %>

<style>
    #header {
        background-color:white;
        color: #1551A0;
        text-align: center;
        padding:5px;
        font-size: 200%;
    }
    
</style>